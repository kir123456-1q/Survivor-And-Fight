# 论文终稿待办清单

> 对应 LaTeX 源稿：`thesis/`（`main.tex` 编入 chapter01–08）  
> 个人信息已更新：刘瀚文 / 2207020509 / 指导教师 董玉坤  
> 最后整理：2026-05-18

---

## 一、已更新项

| 字段 | 内容 | 文件 |
|------|------|------|
| 学生姓名 | 刘瀚文 | `thesis/main.tex` → `\thesisauthor` |
| 学号 | 2207020509 | `thesis/main.tex` → `\thesisstudentid` |
| 指导教师 | 董玉坤 | `thesis/main.tex` → `\thesisadvisor` |

---

## 二、仍须你本人填写

### 2.1 封面与声明（LaTeX）

- [ ] **专业班级**：`thesis/main.tex` 中 `\thesisclass` 仍为「（请填写班级）」
- [ ] **原创性声明 / 版权授权书**：`thesis/frontmatter.tex` 中作者与导师**手写签名**、**日期**
- [ ] **保密论文解密年限**（若不涉密可留空或划掉）：`frontmatter.tex` 授权书处

### 2.2 纸质/PDF 定稿前

- [ ] 用学院 Word/PDF 模板与 `thesis/main.pdf` **逐页比对**字体、行距、页眉（LaTeX 为近似实现）
- [ ] 若学校要求 Word 提交：Pandoc 转 `.docx` 后再套模板样式

---

## 三、内容与数据（高优先级）

### 3.1 性能测试数据

#### 测试关卡 FPS（已记录 ✅）

详见 [`docs/test-level-fps-results.md`](test-level-fps-results.md)，论文表 `tab:test-level-fps-atlas`、`tab:test-level-fps-compare`。

**接入动态图集后（2026-05-18）**

| 波次 | 怪物 | 平均 FPS |
|------|------|----------|
| 1/3 | 10 | 56.74 |
| 2/3 | 100 | 40.72 |
| 3/3 | 1000 | **15.10** |
| 整场 | — | **37.48** |

**对照（图集前）**：整场 39.46；第 3 波 13.32 → 15.10（约 +13.4%）。

#### 五波性能测试（已记录 ✅，2026-05-18）

**位置**：`thesis/chapter07.tex` 表~\ref{tab:test-five-waves}、\ref{tab:perf}

| 波次 | 怪物 | 池 | Worker | 平均 FPS | P95 (ms) |
|------|------|-----|--------|----------|----------|
| 1 | 10 | 开 | 开 | 58.62 | — |
| 2 | 100 | 开 | 开 | 46.84 | — |
| 3 | 1000 | 开 | 开 | 15.14 | — |
| 4 | 1000 | 关 | 关 | 26.16 | 76.50 |
| 5 | 1000 | 开 | 关 | 25.98 | 48.60 |

**对象池消融（第 4–5 波同窗）**：已填入 `tab:perf`。**池+Worker 同窗消融**：未测（论文仅用前五波，第 3 波仅作参考）。

**仍缺**：

- [ ] Worker 同窗对照（预热后）——答辩口径：五波实验未含，见 `chapter07` 第 5 点解读
- [x] 环境元数据表 → `thesis/chapter07.tex` 表 `tab:test-env`
- [x] 摘要与实测对齐 → `thesis/frontmatter.tex`
- [ ] 六张运行图 PNG → 放入 `thesis/figures/`（见下文文件名与拍摄说明）
- [ ] 可选：第 7 张 Performance 截图 `fig-07-performance.png`

### 3.2 系统截图（LaTeX 占位已就绪）

`thesis/chapter07.tex` 已预留 `\includegraphics` 占位；将 PNG 放入 `thesis/figures/` 后重新编译即可。

| 文件名 | 图题 | 拍摄要点（详见助手回复） |
|--------|------|--------------------------|
| `fig-01-start.png` | 开始界面 | StartPanel，含选关入口 |
| `fig-02-runmap.png` | 跑图界面 | DAG 节点、Boss/战斗/休息图标 |
| `fig-03-combat.png` | 战斗场面 | 多怪+弹幕，宜 Level3 第 3–5 波 |
| `fig-04-skill.png` | 技能装配 | Tab 暂停，三法杖槽+Effect 拖拽 |
| `fig-05-reward.png` | 升级奖励 | 三选一奖励面板 |
| `fig-06-restart.png` | 死亡重启 | RestartPanel |
| `fig-07-performance.png`（可选） | Performance | 第 4/5 波 Scripting 或 GC 对比 |

每张图下配 100–200 字图注说明（可在 `chapter07` 各 `\caption` 后扩写）。

### 3.3 正文字数（若学院要求 ≥ 2 万字汉字）

| 统计范围 | 汉字约数 | 说明 |
|----------|----------|------|
| 仅 `main.tex` 编入章节 | ~5 500 | 当前 PDF 正文偏短 |
| 全部 `thesis/chapter*.tex` + `frontmatter` | ~16 300 | 仍差约 3 700 字 |

**做法（二选一或组合）**：

- [ ] 将 `chapter*-extended.tex`、`chapter02-literature.tex`、`chapter05-implementation-case.tex` 等**合并进**对应 `chapter0x.tex`
- [ ] 或在主章扩写第 2、5、7 章（实现细节、测试、相关工作）

统计命令：`node tools/count-thesis-chars.cjs`

---

## 四、附录与测试材料

| 项 | 状态 | 说明 |
|----|------|------|
| 需求–测试用例完整矩阵 | ❌ | 主文仅示例；`chapter03-requirements-detail.tex` 建议终稿附录给全表 |
| 缺陷记录表 | ❌ | `chapter07-extended.tex` 为模板，需替换为真实 Bug |
| 测试结论段落 | ❌ | `chapter07-test-detail.tex` 有填空模板（环境、FPS 提升%、日期） |
| OpenSpec 变更清单 | ❌ | `supplement-conclusion.tex` 提到，但 `appendix.tex` 未收录 |
| PlantUML 类图/时序图 | 可选 | `chapter04-extended.tex` 建议终稿补充 |

---

## 五、功能实现 vs 论文表述（答辩口径）

避免论文/答辩夸大；与仓库一致的说法如下：

| 功能 | 论文中的位置 | 实现情况 |
|------|----------------|----------|
| ECS、战斗、子弹、怪物、升级 | 第 3–5 章 Must | ✅ 已实现 |
| 元菜单、跑图 DAG | 第 3–5 章 Should | ✅ 已实现 |
| Tab 技能装配 UI（三装备栏 + Effect 拖拽） | 第 3、5 章 | ✅ `add-combat-skill-loadout-ui` 基本完成 |
| **法杖三槽 / 法术链式编程 UI** | 第 3 章 Could；第 8 章「待完善」 | ❌ **仅配表与文档**（`docs/config/wand_*.json`），`src/` 无 Wand 运行时 |
| 各 Effect 独立 VFX | 第 7 章已说明不足 | ⚠️ 部分未做 |
| 联机 / 账号 / 支付 | Won't | ❌ 本期不做（符合预期） |
| 局外存档与元进度 | 第 8 章展望 | ❌ 未实现 |

**答辩建议表述**：  
「技能装配与战斗闭环已完成；法杖 spellcraft 完成了数据规格与 OpenSpec 配表，运行时与专用 UI 列为后续工作。」

---

## 六、参考文献与扩展稿

### 6.1 主文（chapter01–08）

- `thesis/references.bib`：约 **20 条真实文献**（SyDRA、Gregory、Cantrell 等）
- 主文 `\cite{}` 与 bib **匹配**，一般可直接用于终稿
- 若学院要求近五年文献比例、外文/中文比例，需自行对照细则增删

### 6.2 勿直接合并、需先改引用的文件

以下文件仍含 `\cite{kw_...}` 等**已删除的占位键**，合并进主章前须改引用或删段：

- `thesis/supplement-conclusion.tex`（且写明「20 条占位」——**已过时**）
- `thesis/chapter01-extended.tex`
- `thesis/chapter02-literature.tex`
- `thesis/chapter03-requirements-detail.tex` 等带 `kw_*` 的扩展稿

`supplement-conclusion.tex` **未** `\input` 进 `main.tex`，不会进入当前 PDF。

---

## 七、答辩配套（仓库外）

| 材料 | 状态 | 参考 |
|------|------|------|
| 答辩 PPT（15–20 页） | 未在仓库 | `supplement-conclusion.tex` § 答辩材料 |
| 演示视频（3–5 分钟，含跑图+技能装配） | 未在仓库 | 同上 |
| OpenSpec 变更列表打印 | 可选 | `openspec/changes/` 目录 |
| 演示顺序建议 | — | `thesis/chapter01-background-extra.tex` |

---

## 八、建议修订顺序

1. 补全 **班级**、声明签名与日期  
2. ~~跑性能实验 / 填表 7.2 / 摘要~~（已完成，见 `chapter07`）  
3. 截 **6 张** PNG 放入 `thesis/figures/` 并编译 PDF  
4. 合并扩展章或扩写，达到 **2 万字**（若学院硬性要求）  
5. 补附录：需求–测试矩阵、缺陷表（可选 OpenSpec 清单）  
6. 导师审阅 → 与学院模板比对 → 查重定稿  

---

## 九、常用命令

```bash
# 论文字数统计
node tools/count-thesis-chars.cjs

# 编译 PDF（在 thesis/ 目录）
xelatex main.tex
bibtex main
xelatex main.tex
xelatex main.tex

# 同步配表到运行目录
powershell -File tools/sync-config-to-bin.ps1

# 校验 OpenSpec 变更
npx openspec validate <change-id> --strict --no-interactive
```

---

## 十、相关文件索引

| 用途 | 路径 |
|------|------|
| 主入口 | `thesis/main.tex` |
| 封面/摘要/声明 | `thesis/frontmatter.tex` |
| 性能表（待填） | `thesis/chapter07.tex` |
| 性能实验指南（扩展） | `thesis/chapter06-benchmark-guide.tex` |
| 截图与测试模板（扩展） | `thesis/chapter07-test-detail.tex` |
| 初稿补充说明（过时部分勿照搬） | `thesis/supplement-conclusion.tex` |
| 参考文献 | `thesis/references.bib` |
| 学院模板提取 | `docs/thesis-template-requirements.txt` |
