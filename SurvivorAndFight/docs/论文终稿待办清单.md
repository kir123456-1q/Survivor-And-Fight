# 论文终稿待办清单

> 源稿：`thesis/main.tex`（chapter01–08 + appendix）  
> 个人信息：刘瀚文 / 2207020509 / 软件工程2205班 / 指导教师 董玉坤  
> 最后更新：2026-05-21

---

## 一、已完成（自动化 / 文稿）

| 项 | 状态 |
|----|------|
| 封面题目与 `\thesistitle` 统一 | ✅ `frontmatter.tex` |
| 五波性能数据、对象池消融 P95 | ✅ `chapter07` |
| 动态图集前三波对比 | ✅ |
| 六张运行截图 `fig-01`～`fig-06` | ✅ `figures/` + `Png/` |
| 系统时序图 Mermaid | ✅ `figures/system-sequence.mmd` → PNG |
| 分层架构图 Mermaid | ✅ `figures/system-layers.mmd` → PNG |
| Tab 装配时序图 Mermaid | ✅ `figures/skill-tab-sequence.mmd` → PNG |
| 测试环境 Git 版本 | ✅ `build.ps1` → `build-info.tex` |
| 附录需求—测试映射、缺陷表 | ✅ `appendix.tex` |
| 未编入扩展稿归档 | ✅ `thesis/_archive/` |
| 参考文献 `references.bib` | ✅ 真实条目，非占位关键词 |
| LaTeX → Word（Pandoc） | ✅ `tools/tex-to-docx.py` → `thesis/main-from-tex.docx` |

编译：`cd thesis` → `.\build.ps1`（含 Png 同步、Mermaid 导出、XeLaTeX×3、BibTeX）

导出 Word（与 `main.tex` 同步，需 Pandoc 3.x）：

```powershell
cd thesis
.\export-docx.ps1
# 或：py -3 tools/tex-to-docx.py -o thesis/main-from-tex.docx
```

导出后请在 Word 中套用学院模板、重新生成目录/图目录，并核对公式与表格。

---

## 二、须你本人完成（高优先级）

### 2.1 声明签名（无法代填）

`thesis/frontmatter.tex` 已预留：

- 原创性声明：论文作者签名、指导教师确认签名、**日期**（下划线手写）
- 版权授权书：论文作者签名、指导教师签名、**日期**；保密解密年限（不涉密可留空）

**操作步骤：**

1. 运行 `.\build.ps1` 得到 `main.pdf`
2. **纸质提交**：打印声明页 → 手写签名与日期 → 扫描或装订进终稿
3. **电子提交**：若学院接受，将签字页扫描为 PDF 插入；或使用学院规定的电子签章流程

### 2.2 字数确认（扩写暂缓）

| 统计范围 | 汉字约数 | 命令 |
|----------|----------|------|
| **main.tex 实际编入（= PDF 正文）** | **约 12 815** | `node tools/count-thesis-compiled.cjs` |
| 仓库全部 `chapter*.tex`（含归档） | 约 22 000+ | `node tools/count-thesis-chars.cjs` |

若学院硬性要求 **≥ 2 万字**：当前缺口约 **7 200 字**。  
**按你的要求：暂不扩写、暂不合并 `chapter02-literature`。** 答辩前与导师确认是否可用附录/外文翻译补足，或批准当前篇幅。

### 2.3 定稿前格式

- [ ] 与学院 Word/PDF 模板**逐页比对**字体、行距、页眉
- [ ] 查重、导师签字页、装订要求

---

## 三、其他说明

| 项 | 状态 |
|----|------|
| ECS 类图（PlantUML） | 非必须，已有分层图与 Mermaid 时序图 |
| 正文字数扩写第 2/5/7 章 | **暂缓** |

（已从正文删除：`fig-07-performance.png`、Worker 第六段同窗实验及相关“后续工作”表述。）

---

## 四、答辩口径（与代码一致）

| 功能 | 状态 |
|------|------|
| ECS、战斗、跑图、Tab 技能装配、对象池 | ✅ |
| 法杖三槽 / spellcraft 运行时 UI | ❌ 配表与文档为主 |
| 部分 Effect 独立 VFX | ⚠️ 逻辑有，特效未全做 |
| 联机 / 账号 / 支付 | ❌ Won't |

---

## 五、相关路径

| 用途 | 路径 |
|------|------|
| 主入口 | `thesis/main.tex` |
| 编译脚本 | `thesis/build.ps1` |
| Mermaid 源图 | `thesis/figures/*.mmd` |
| 截图源 | `thesis/Png/fig-*.png` |
| 历史扩展稿（勿编入主 PDF） | `thesis/_archive/` |
| 编入正文字数统计 | `tools/count-thesis-compiled.cjs` |
| LaTeX 导出 Word | `tools/tex-to-docx.py`、`thesis/export-docx.ps1` |
| Word 草稿输出 | `thesis/main-from-tex.docx` |
| PDF 转 Word（备用，易失真） | `tools/pdf-to-docx.py` |
