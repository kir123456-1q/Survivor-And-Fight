\chapter{系统总体设计}
\label{chap:architecture}

\section{设计原则}

\begin{enumerate}[label=(\arabic*)]
  \item \textbf{数据与逻辑分离}：组件存数据，系统执逻辑；配表驱动参数\cite{gregory2018game}。
  \item \textbf{单一职责}：\texttt{BulletSystem} 负责子弹与碰撞，\texttt{EffectExecutor} 负责效果语义映射。
  \item \textbf{可验证}：核心模块提供单元验证脚本（公式解析、ECS 核心、属性修饰器）。
  \item \textbf{渐进复杂度}：轻量 ECS；Worker 仅在规模达标时启用。
  \item \textbf{低耦合}：参考游戏引擎子系统解耦经验，避免 UI 直接操作战斗节点\cite{ullmann2023coupling}。
\end{enumerate}

\section{系统分层架构}

\begin{figure}[htbp]
  \centering
  \fbox{\parbox{0.92\textwidth}{
    \centering
    \textbf{表现层}：Laya 预制体、UI、血条、相机跟随\\
    $\downarrow$\\
    \textbf{UI 控制层}：MVC、UIStackManager、各 Panel\\
    $\downarrow$\\
    \textbf{游戏逻辑层}：ECS Systems、Bullet、RunMap、MetaFlow\\
    $\downarrow$\\
    \textbf{领域服务层}：ConfigBootstrap、FormulaParser、CombatDataBridge\\
    $\downarrow$\\
    \textbf{基础设施层}：EcsWorld、ComponentStore、defines、Worker 协议
  }}
  \caption{系统分层架构示意}
  \label{fig:layers}
\end{figure}

\texttt{Main.ts} 初始化输入、构造 \texttt{SimpleEcsDemo}，按配置进入元菜单或战斗，在 \texttt{frameLoop} 中更新逻辑与生存计时。

\section{ECS 调度与战斗数据流}

系统分组：\texttt{input}（操控）、\texttt{logic}（属性、移动、技能、刷怪、经验等）、\texttt{render}（视图、血条、HUD）。\texttt{GameSession.paused} 控制战斗暂停。

战斗帧流程：\texttt{CombatDataPrepareSystem} 采集快照 $\rightarrow$ 可选 Worker 执行 \texttt{processCombatFrame} $\rightarrow$ 主线程应用追逐/分离结果 $\rightarrow$ \texttt{BulletSystem} 更新碰撞。该设计与 Cantrell 等将重计算置于引擎逻辑管线、渲染与交互分离的思路一致\cite{cantrell2018unity,bott2014unreal}。

\section{元游戏与配表模型}

\texttt{MetaFlowController} 协调界面与战斗入口；\texttt{RunMapState} 维护 DAG 进度。逻辑数据关系：\texttt{Character}、\texttt{Skill} 1:N \texttt{SkillEffect}，\texttt{SkillEffect} 可关联 \texttt{Bullet}；跑图节点通过边构成 DAG。

\section{部署与容错}

H5 静态发布至 \texttt{bin/}；无服务端数据库。配表缺失时使用默认常量；Worker 失败回退主线程；跑图生成失败使用保底图；预制体加载失败可用占位碰撞体调试。

\section{本章小结}

本章给出分层架构、ECS 调度、战斗数据流与元游戏设计，下一章详述模块实现。
