## 1. 规格与数据
- [ ] 1.1 新增等级表 `level_table.json`：等级、所需经验、可解锁层级标签。
- [ ] 1.2 新增升级池表 `upgrade_pool_table.json`：升级项 id、权重、前置、互斥、重复上限、展示文案 key。
- [ ] 1.3 新增升级效果表 `upgrade_effect_table.json`：效果类型（新子弹类型/子弹数/穿透/伤害/攻速/持续时间）与参数。
- [ ] 1.4 更新 `tables.registry.json` 注册新增配表，并补充字段说明文档。

## 2. 运行时系统
- [ ] 2.1 实现经验与等级系统（击杀加经验、升级判定、多级连升）。
- [ ] 2.2 实现随机升级候选生成（默认 3 选 1，权重随机+条件过滤）。
- [ ] 2.3 实现升级应用系统，将升级结果写入运行时成长状态并持久到战斗局内。

## 3. 战斗系统整合优化
- [ ] 3.1 改造子弹发射流程，支持“新增子弹类型”与“单次多发子弹”。
- [ ] 3.2 改造子弹参数计算，支持穿透、伤害、速度、持续时间等升级叠加。
- [ ] 3.3 增加上限保护与性能策略（子弹总量上限、发射间隔下限、对象池优先回收）。

## 4. UI 与交互
- [ ] 4.1 新增升级选择面板（候选卡片、描述、确认交互）。
- [ ] 4.2 接入升级时战斗暂停/恢复流程，确保升级选择期间状态一致。
- [ ] 4.3 为升级项接入图标与文本 key，支持后续美术替换与本地化扩展。

## 5. 资源与联调
- [ ] 5.1 接入首批升级图标资源（至少覆盖子弹类型、子弹数量、穿透、伤害）。
- [ ] 5.2 接入首批新子弹预制体（可先占位，后替换正式表现）。
- [ ] 5.3 联调验证：连续升级、重复升级边界、升级后战斗表现与性能稳定性。

## 6. 验证
- [ ] 6.1 运行流程验证：击杀 -> 升级 -> 随机候选 -> 选择 -> 战斗数值生效。
- [ ] 6.2 规则验证：互斥升级不同时出现；达到重复上限后不再入池。
- [ ] 6.3 回归验证：未升级情况下保持现有战斗行为不退化。
- [ ] 6.4 `openspec validate add-battle-level-and-random-upgrades --strict --no-interactive` 通过。
